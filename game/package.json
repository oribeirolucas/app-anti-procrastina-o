{
  "name": "street-embaixadinhas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "playtest": "node tests/playtest.mjs",
    "shots": "node tests/shots.mjs . && node tests/shots-meta.mjs .",
    "metatest": "node tests/meta.mjs",
    "test": "node tests/playtest.mjs && node tests/meta.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "~5.7.2",
    "vite": "^6.0.0"
  }
}
